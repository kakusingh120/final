<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Indian Law Summarizer</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    input[type="range"]::-webkit-slider-thumb {
      background-color: #ef4444;
    }
  </style>
</head>
<body class="bg-[#f4eede] min-h-screen flex items-center justify-center py-10 px-4">
    <div class="w-full max-w-3xl bg-gray-100 shadow-lg rounded-2xl p-8">
        <h1 class="text-3xl font-bold text-sky-500 mb-4 flex items-center gap-2">
        <span class="inline-block w-6 h-6 bg-gradient-to-r from-green-400 via-blue-500 to-purple-500 rounded-sm"></span>
        Indian Law Summarizer - Text, Image or PDF
        </h1>
        <p class="text-gray-600 mb-6">
        Upload a legal clause or article in any format (text/image/PDF) to generate a simplified summary.
        </p>

        <!-- File Upload Area -->
        <div class="mb-6">
        <label class="block text-sm font-medium text-gray-700 mb-2">Upload a PDF or Image</label>
        <div id="drop-area" class="flex flex-col items-center justify-center border-2 border-dashed border-gray-300 rounded-xl p-6 cursor-pointer hover:border-blue-500 transition relative">
            <input type="file" id="file-upload" accept=".pdf, .png, .jpg, .jpeg" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
            <p class="text-center text-sm text-gray-500 pointer-events-none">
            Drag and drop file here or <span class="text-blue-600 font-medium">browse files</span>
            </p>
            <p class="text-xs text-gray-400 mt-2 pointer-events-none">Limit 200MB per file · PDF, PNG, JPG, JPEG</p>
            <div id="file-name" class="text-sm text-green-600 mt-3 hidden"></div>
        </div>
        </div>

        <!-- Raw Text Input -->
        <div class="mb-6">
        <label class="block text-sm font-medium text-gray-700 mb-2">Or enter raw legal text</label>
        <textarea id="legal-text" rows="6" placeholder="Paste legal clause here..."
            class="w-full rounded-lg border border-gray-300 p-4 focus:outline-none focus:ring-2 focus:ring-blue-400 transition"></textarea>
        </div>

        <!-- Language Selection -->
        <div class="mb-6">
        <label class="block text-sm font-medium text-gray-700 mb-2">Output Language</label>
        <select id="language-select" class="w-full rounded-lg border border-gray-300 p-3 focus:outline-none focus:ring-2 focus:ring-blue-400">
            <option value="english">English</option>
            <option value="hindi">Hindi</option>
            <option value="tamil">Tamil</option>
            <option value="bengali">Bengali</option>
            <option value="marathi">Marathi</option>
            <option value="telugu">Telugu</option>
        </select>
        </div>

         <!-- Max Summary Length -->
         <div class="mb-6">
           <label class="block text-sm font-medium text-gray-700 mb-2">Max Summary Length</label>
           <input type="range" id="maxLength" min="50" max="200" value="176" class="w-full accent-red-500">
           <div class="text-right text-sm text-red-600 mt-1" id="maxLengthLabel">176</div>
         </div>
     
         <!-- Min Summary Length -->
         <div class="mb-6">
           <label class="block text-sm font-medium text-gray-700 mb-2">Min Summary Length</label>
           <input type="range" id="minLength" min="10" max="100" value="30" class="w-full accent-red-500">
           <div class="text-right text-sm text-red-600 mt-1" id="minLengthLabel">30</div>
         </div>
     

        <!-- Submit Button -->
        <button id="submit-btn" class="w-full bg-blue-600 text-white py-3 rounded-lg font-semibold hover:bg-blue-700 transition">
            Generate Summary
        </button>
    
        <!-- Output Preview -->
        <div id="output" class="mt-6 hidden bg-gray-100 p-4 rounded-lg border border-gray-300 text-gray-700">
            <strong>Summary Output:</strong>
            <p id="output-text" class="mt-2"></p>
        </div>

        
    </div>


   <script src="/javascripts/Doc.inter.js"></script>
</body>
</html>